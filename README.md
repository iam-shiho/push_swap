*This project has been created as part of the 42 curriculum by swaragay, ttatsuno.*


# Push_swap — Adaptive Sorting with Two Stacks

## Description
`push_swap` は、2つのスタック（`a` と `b`）と制限された一連の操作命令（`sa`, `pb`, `ra` など）のみを使用し、ランダムに与えられた整数列を最小の手数（操作回数）でソートするプログラムです。
本プロジェクトでは、アルゴリズムの時間・空間複雑度（Big-O）の深い理解と、与えられた入力データの「無秩序度（Disorder Metric）」に応じて最適なアルゴリズムを動的に選択する**適応型アルゴリズム（Adaptive Algorithm）**の実装を目的としています。

---

## Technical Choices & Algorithms
//ttatsuno パースの部分の解説
### パース


### 座標圧縮
アルゴリズムの効率化（操作数が減る）、受け取ったvalueは保持したまま最小値から新しくインデックスをつけてあげる。

```text
    1      5  8 10         32                                99
  ──│──────│──│──│──────────│─────────────────────────────────│──

                                ⬇

    0   1   2   3   4   5
  ──│───│───│───│───│───│──
```
//ttatsuno アルゴリズムの説明や、実装の工夫点　既存のものの場合はあとで詳細に説明するため、使用理由を書く（計算が早いためなど）

### Disorder
本プログラムは、実行時にスタックの「無秩序度（Disorder）」を以下の数式（0.0〜1.0）に基づいて算出します。
* $\text{disorder} = \frac{\text{mistakes}}{\text{total\_pairs}}$
※mistakes =　ノードのvalueを比べた際前の数字の方が大きいときをカウントした総数
※total =　ノードの総数

###　アルゴリズム
####  operations
| 関数名 | 説明 |
|---|---|
| **pa** | `stack_a`の先頭を`stack_b`の先頭に移動する |
| **pb** | `stack_b`の先頭を`stack_a`の先頭に移動する |
| **ra** |　`stack_a` の全要素を上に1つズラす（先頭は末尾へ) |
| **rb** |  `stack_b` の全要素を上に1つズラす（先頭は末尾へ） |
| **rr** | raとrbを同時に行う |
| **rra** | `stack_a` の全要素を下に1つズラす（末尾は先頭へ） |
| **rrb** | `stack_b` の全要素を下に1つズラす（末尾は先頭へ） |
| **rrr** | rraとrrbを同時に行う |
| **sa** | `stack_a` の最初の2つの要素を入れ替える |
| **sb** | `stack_b` の最初の2つの要素を入れ替える |

#### 1. Simple Algorithm ($O(n^2)$)
* **選択フラグ**: `--simple`
* **使用アルゴリズム**: `選択ソート`
* **解説と空間・時間計算量**:
| 関数名 | 戻り値 |　説明 |
|---|---|---|
| **simple** | void | 最小値をbに入れ、ソートする関数 |
| **put_index** | int | 前からの順番をメンバ|
| **ra** |　|`stack_a` の最初の2つの要素を入れ替える |
| **rb** |  |`stack_a` の全要素を上に1つズラす（先頭は末尾へ） |
| **rr** | |`stack_a` の最初の2つの要素を入れ替える |
| **rra** | |`stack_a` の全要素を下に1つズラす（末尾は先頭へ） |
| **rrb** | |`stack_a` の全要素を下に1つズラす（末尾は先頭へ） |
| **rrr** | |`stack_a` の全要素を下に1つズラす（末尾は先頭へ） |
| **sa** | |`stack_a` の全要素を下に1つズラす（末尾は先頭へ） |
| **sb** | |`stack_a` の全要素を下に1つズラす（末尾は先頭へ） |

`計算量`
未確定の要素の中から最小値を探索する際、$1$回目の探索で $N$ 個、$2$回目で $(N-1)$ 個、$3$回目で $(N-2)$ 個... と、**毎回すべての残りの要素を比較する**必要があります。
比較の総数が要素数 $N$ の $2$ 乗に比例して増えていくため、$O(N^2)$ となります。

`操作数を減らすために`
先頭からのindexをつけ、過半数以下なら上に１つずつ先頭に持っていく
過半数なら下から先頭に持っていく

```text
ステップ 1：最小値の探索と回転の最適化たとえば、スタック $A$ に [ 4, 1, 5, 2, 3 ] が入っているとします（要素数 $N = 5$）。
【初期状態】            【最小値 (1) の位置を特定】
   stack_a               stack_a    index   距離の計算
 +---------+           +---------+
 |    4    |           |    4    |   idx:0  （距離 0）
 |    1    |  <-- 最小  |    1    |   idx:1  （距離 1 ➔ ra が速い！）
 |    5    |           |    5    |   idx:2
 |    2    |           |    2    |   idx:3
 |    3    |           |    3    |   idx:4  （過半数より下なら rra）
 +---------+           +---------+

ステップ 2：`move_top`で最小値を先頭に持ってくる ➔ pb
※最小値 1 は index: 1（過半数以下）にあるので、ra を1回実行して先頭に移動させ、pb でスタック $B$ に移動します。
  stack_a               stack_a               stack_b
+---------+           +---------+           +---------+
|    1    |  <--先頭   |    5    |           |    1    |  <-- 最小値キープ
|    5    |           |    2    |           +---------+
|    2    |           |    3    |
|    3    |           |    4    |
|    4    |           +---------+
+---------+

ステップ 3：これを繰り返し、$A$ の要素数が全て $B$ に移動し終わるまでこの操作を繰り返します。スタック $B$ には「大きい順（降順）」に積み上がっていくため、最後に pa で戻すだけでスタック $A$ は「小さい順（昇順）」に整列します。
【Bへ退避完了時】                  【pa を連打して戻す】
 stack_a      stack_b               stack_a      stack_b
+-------+   +-------+              +-------+   +-------+
|       |   |   5   |              |   1   |   |       |
+-------+   |   4   |  === pa ==>  |   2   |   |       |  ✨ ソート完了！
            |   3   |              |   3   |   |       |
            |   2   |              |   4   |   |       |
            |   1   |              |   5   |   |       |
            +-------+              +-------+   +-------+
```


#### 2. Medium Algorithm ($O(n\sqrt{n})$)
* **選択フラグ**: `--medium`
* **使用アルゴリズム**: ` チャンク分割ソート`
* **解説と空間・時間計算量**:
`探索する数を√ｎ個ずつに分割してソートする`
1. 探索する数を√ｎ個ずつに分ける
  ex: ノードが１００の場合＝$\sqrt{10}$　
  探索する数字を10個ずつに分ける　0~9/10~19/20~29/30~39/40~49/50~59/60~69/70~79/80~89/90~99
2. stack_aを探索してまず0~9内の数字（座標圧縮されたインデックス）をさがす
3. みつけたらstack_bにいれていく
4. Bの中から最大値を探してAをソートしていく

`計算量`
全体の要素数を $\sqrt{N}$ 個ずつの範囲に分割することで、$A$ から次に送る数字を探す際の手間が、最大 $N$ 回から最大 $\sqrt{N}$ 回へ激減します。全 $N$ 個の要素に対してこの「最大 $\sqrt{N}$ 回の探索」を行うため、$N \times \sqrt{N} =$ $O(N \sqrt{N})$ に抑えられます。

`操作数を減らすために`
bに送る際に数を数えてその数が偶数だった場合は上に奇数だった場合はraをして下に入れる　最上位にいち早く持っていけるように

```text
ステップ 1：座標圧縮とチャンクの決定要素数 $N = 100$ の場合、10個ずつのブロック（チャンク） に分けます。（値はすべて $0 \sim 99$ に座標圧縮されている前提です）
 [チャンク 0] :  0 ～  9  <-- まずはこのグループだけを A から探す！
 [チャンク 1] : 10 ～ 19
 [チャンク 2] : 20 ～ 29
   ...

ステップ 2：$A$ から範囲内の数字を探して $B$ へ送る（振分最適化）スタック $A$ の上から巡回し、「現在のチャンク範囲内（例: $0 \sim 9$）」の数字 を見つけたら ra や rra で先頭に持ってきて $B$ へ移動（pb）。このとき、$B$ へ送った通算個数が偶数か奇数か で配置を変えます。
【ケース A：偶数番目の取得】                【ケース B：奇数番目の取得】
  そのまま pb（Bの上に置く）                  pb したあと rb（Bの下へ送る）

   stack_a       stack_b                    stack_a       stack_b
 +---------+   +---------+                +---------+   +---------+
 |  [ 3 ]  |   |    5    | (上)           |  [ 8 ]  |   |    3    | (上)
 |    42   |   |    2    |                |    42   |   |    5    |
 |   ...   |   +---------+                |   ...   |   |    2    |
 +---------+                                +---------+   +---------+
      |                                          |
      v  pb                                      v  pb ➔ rb
 +---------+   +---------+                +---------+   +---------+
 |    42   |   |  [ 3 ]  | (上)           |    42   |   |    5    | (上)
 |   ...   |   |    5    |                |   ...   |   |    2    |
 +---------+   |    2    |                +---------+   |  [ 8 ]  | (下)
               +---------+                              +---------+
💡　チャンク $0 \sim 9$ の中で、上のエリアと下のエリアにバラけるため、$B$ の中で数値が固まらず、全要素を $B$ に送り終わったあとに $A$ へ戻す（降順に並べ直す）時の操作数（rb/rrb）が最小限で済む ようになります！

ステップ 3：全チャンクを送り終えたあと$B$ の中は「大体ソートされた山」になります。
            stack_b
          +---------+
  (上)    | 90 ~ 99 |  <-- 最後のチャンク（大きい数）
          | 70 ~ 89 |
          | ...     |
          | 10 ~ 29 |
  (下)    |  0 ~  9 |  <-- 最初のチャンク（小さい数）
          +---------+

ステップ 4：$B$ から最大値を順に $A$ へ戻す（pa）仕上げに、$B$ の中で一番大きい数字（例えば最初は $99$、次は $98$ ...）の位置（index）を計算し、rb または rrb で先頭に呼び寄せて pa で $A$ に戻します。すでに $B$ の中が大まかに整列しているため、移動距離が常に非常に短くなり、操作数が激減します！   stack_b 内で最大値(99)を探す ➔ rb / rrb でトップへ ➔ pa で A へ戻す
    stack_a       stack_b                    stack_a       stack_b
  +---------+   +---------+                +---------+   +---------+
  |         |   |  [99]   |                |   99    |   |   98    |
  |         |   |    98   |   == pa ==>    |   ...   |   |   ...   |
  |         |   |   ...   |                +---------+   +---------+
  +---------+   +---------+               ✨ A にキレイな昇順ができる！
```

#### 3. Complex Algorithm ($O(n \log n)$)
* **選択フラグ**: `--complex`
* **使用アルゴリズム**: `基数ソート(Radix Sort)`
* **解説と空間・時間計算量**:
`0と1で山分けしていくソート`
1. 座標の桁数を数える（）
2. 各数字の1bitを比べてソートしていく
  1→stack_aのまま（raをして先頭を変える）
  0→stack_bに移動
3. すべてのノードが比べ終わったらbに入っているものを上からaに戻す
4. 右シフトしてビットを１つずらす
5. 再度０１で振り分ける
それを座標圧縮の最大値のbit数分繰り返す

`計算量`
事前に座標圧縮を行っているため、スタック内の数字の最大値は要素数と同じ $N$（正確には $N-1$）に抑えられます。
値 $N$ を2進数で表現したときの桁数（ビット数）は数学的に $\log_2 N$ となるため、「何回ビットを調べるループを回せばいいか」は $\log N$ 回に限定されます。
$1$ ビット調べるごとに全 $N$ 個の要素を振り分ける処理を行うため、全体の処理回数は「$N$ 個 $\times \log N$ 桁分」となり、
`操作数を減らすために`
座標圧縮することによって最大値がノードの総数となり、比べるbit数が減る。

```text
例として、座標圧縮済みの 4つの要素 [ 3, 1, 0, 2 ]（2ビットで表現可能）をソートする流れを追ってみます。

ステップ 1：【1巡目】最下位ビットでスタック $A$ の先頭要素の bit 0（右端の桁）をチェックします。1 の場合 ➔ ra （そのまま $A$ の後ろに回す）0 の場合 ➔ pb （$B$ へ送る）
【初期状態】                 【bit 0 のチェックと移動】
   stack_a                      stack_a                     stack_b
 +---------+                  +---------+                 +---------+
 | 3 (11)  |  1 ➔ ra          | 3 (11)  |                 | 2 (10)  |               0 ➔ pb
 | 1 (01)  |  1 ➔ ra          | 1 (01)  |                 | 0 (00)  |               0 ➔ pb
 | 0 (00)  |  0 ➔ pb          +---------+                 +---------+
 | 2 (10)  |  0 ➔ pb          (1 のグループ)               (0 のグループ)
 +---------+

➔ 1巡目の判定が終わったら、$B$ に移動させた 0 のグループを丸ごと $A$ の上に戻します。
   stack_a                     stack_b
 +---------+                 +---------+
 | 0 (00)  |                 |         |  <-- 全部 pa で移動
 | 2 (10)  |                 +---------+
 | 3 (11)  |
 | 1 (01)  |
 +---------+

ステップ 2：【2巡目】右シフト（>> 1）して次のビットで振り分けを行います。
   stack_a                      stack_a                     stack_b
 +---------+                  +---------+                 +---------+
 | 0 (00)  |  0 ➔ pb          | 2 (10)  |                 | 1 (01)  |  0 ➔ pb
 | 2 (10)  |  1 ➔ ra          | 3 (11)  |                 | 0 (00)  |  0 ➔ pb
 | 3 (11)  |  1 ➔ ra          +---------+                 +---------+
 | 1 (01)  |  0 ➔ pb          (1 のグループ)               (0 のグループ)
 +---------+
➔ 再び $B$ から $A$ へ戻す（pa）   stack_a
 +---------+
 | 0 (00)  |  (値: 0)
 | 1 (01)  |  (値: 1)
 | 2 (10)  |  (値: 2)   ✨ 完全ソート完了！
 | 3 (11)  |  (値: 3)
 +---------+
```

#### 4. Custom Adaptive Algorithm (デフォルト)
* **選択フラグ**: `--adaptive` (またはフラグなし時のデフォルト挙動)
* **設計思想と閾値の正当性**:
  無秩序度に応じて以下の閾値で上記の各アルゴリズムを呼び出します。
  * **Low disorder ( $< 0.2$ )**: $O(n^2)$ の手法を適用。
  * **Medium disorder ( $0.2 \le \text{disorder} < 0.5$ )**: $O(n\sqrt{n})$ の手法を適用。
  * **High disorder ( $\ge 0.5$ )**: $O(n \log n)$ の手法を適用。

---

## Contribution & Collaboration
* **[ttatsuno]**:
  * 担当範囲: ``
* **[swaragay]**:
  * 担当範囲: `アルゴリズムの実装（simple,medium,complex,adaptive）,bench(disorderの作成,出力),dprintf,READMEの作成,`


---

## Instructions

### コンパイル
プロジェクトのビルドには `cc` と `Makefile` を使用します。
```bash
# push_swapの作成
make


# クリーンアップ
make clean
make fclean
make re

```

### 実行例

```bash
# デフォルト（Adaptive）モードでの実行
./push_swap 2 1 3 6 5 8

# フラグを使用しての実行
./push_swap --simple 1 4 2 3 5
./push_swap --medium 10 4 2 9 1
./push_swap --complex 4 67 3 87 23

# エラーケースの挙動（標準エラー出力に "Error" を出力）
./push_swap 3 2 3

```

### --bench

`--bench` フラグを有効にすると、ソート用の命令（標準出力）に加えて、以下の分析メトリクスを **標準エラー出力** に出力します。

```bash
# チェッカーで正当性確認(checker_linuxをchmodしておく)
chmod 755 checker_linux
$ ARG="4 67 3 87 23"; ./push_swap --bench $ARG 2> bench.txt | ./checker_linux $ARG //エラー出力をbench.txtに出力
OK
$ cat bench.txt
[bench] disorder: 40.00%
[bench] strategy: Adaptive / O(n log n)
[bench] total_ops: 13
[bench] sa: 0  sb: 0  ss: 0  pa: 5  pb: 5
[bench] ra: 2  rb: 1  rr: 0  rra: 0  rrb: 0  rrr: 0

```

---

## Resources & AI Usage

### References & Resources

* Donald Knuth, *The Art of Computer Programming* (Big-O Notation and Sorting Algorithms)
* **[ttatsuno]**:
* **[swaragay]**:
  * [ソートアルゴリズム](https://www.study-pg.com/c4-basic/bit-shifts-operators/)
  * [計算量](https://tukumolog.com/what-is-computational-complexity/)
  * [座標圧縮の解説](https://algo-logic.info/coordinate-compress/) `min_index`
  * [選択ソート](https://ja.wikipedia.org/wiki/%E9%81%B8%E6%8A%9E%E3%82%BD%E3%83%BC%E3%83%88) `simple(選択ソート)`
  * [チャンクソート](https://www.linkedin.com/pulse/push-swap-art-sorting-two-stacks-anass-outiskte-bjsje) `medium(チャンクソート)`
  * [基数ソートの仕組み](https://tukumolog.com/radix-sort-introduction/) `complex(基数ソート)`
  * [ビットシフトとシフト演算子の基本](https://www.study-pg.com/c4-basic/bit-shifts-operators/) `complex(基数ソート)　bit演算の処理`
### AI Tool Usage Disclosure
* **使用したタスク**:
* `README: フォーマット, アルゴリズム等の図解`
